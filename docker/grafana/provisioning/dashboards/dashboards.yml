apiVersion: 1

providers:
  - name: network-monitor-dashboards
    orgId: 1
    folder: Network Monitor
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
